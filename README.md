- v2: not using #heuristic, v2 is main.

- v3: using #heuristic, is in developed.
    - v3.1 is ready

how to run:
each encoding is associated with a file solver.sh

For example:

`bash trip-noRQtime-formulation/v3-greedy-based/encoding_v3.2/solver_NY.sh`
